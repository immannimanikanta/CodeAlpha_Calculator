Task 1: Calculator Program

### Description
A straightforward terminal-based application designed to process basic mathematical expressions using structured decision blocks.

### Key Implementation Details
* Uses a standard `switch-case` evaluation block for fast structural execution.
* Employs zero-division detection rules to prevent program crashes (`SIGFPE` signals) during runtimes.
